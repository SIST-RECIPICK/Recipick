<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.mapper.RefridgeMapper">
  <insert id="registerData" parameterType="com.sist.web.vo.RefridgeVO">
    INSERT INTO Refrigerator(id,users_id,ingredient_id,created_at)
    VALUES(ref_id_seq.nextval,#{users_id},#{ingredient_id},SYSDATE)
  </insert>
  
  <!-- VO에 ingredientVO 객체자체를 필드로 가져다 놓아서 resultmap 씀-->
  <resultMap id="refridgeResultMap" type="com.sist.web.vo.RefridgeVO">
        <id property="id" column="id"/>
        <result property="users_id" column="users_id"/>
        <result property="ingredient_id" column="ingredient_id"/>
        <result property="created_at" column="created_at"/>
        <association property="ingredient" javaType="com.sist.web.vo.IngredientVO">
            <id property="id" column="ing_id"/>
            <result property="category_name" column="category_name"/>
            <result property="ingredient_name" column="ingredient_name"/>
        </association>
    </resultMap>
  
  <select id="getMyFridge" parameterType="int" resultMap="refridgeResultMap">
        SELECT r.id, r.users_id, r.ingredient_id, r.created_at,
               i.id AS ing_id, i.category_name, i.ingredient_name
        FROM Refrigerator r
        JOIN ingredient i ON r.ingredient_id = i.id
        WHERE r.users_id = #{usersId}
    </select>
</mapper>